# Assets

UI screenshots for the submission README and demo materials.

```text
assets/
└── screenshots/
    ├── dashboard.png        # Main scan screen
    ├── risk_overlay.png     # Bounding boxes and severity labels
    ├── safe_share.png       # Redacted output / comparison slider
    └── snapdragon_panel.png # Runtime/backend telemetry panel
```

Capture these on demo day (see `docs/SUBMISSION.md` checklist) and commit
them here. Screenshots must show only synthetic demo data.
